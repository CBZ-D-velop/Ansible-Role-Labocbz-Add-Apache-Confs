    # Redirect all HTTP traffic to HTTPS
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}:{{ add_apache_confs__https_listen_port }}%{REQUEST_URI} [L,R=301]
